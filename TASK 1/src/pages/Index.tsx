import ChatBot from '@/components/ChatBot';

const Index = () => (
  <div className="min-h-svh flex items-center justify-center md:p-6 bg-background">
    <ChatBot />
  </div>
);

export default Index;
